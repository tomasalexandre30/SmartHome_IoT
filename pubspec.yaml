name: smartspace
description: SmartSpace - Sistema de Gestão Inteligente de Espaços
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.31.15
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  permission_handler: ^11.1.0
  fl_chart: ^0.66.2
  intl: ^0.19.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
